version: "3.9"

services:
  firebase-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: firebase-api
    ports:
      - "90:90"
    environment:
      NODE_ENV: production
    restart: unless-stopped

